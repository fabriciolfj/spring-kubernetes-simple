#não usar, vamos usar o secret
apiVersion: v1
kind: ConfigMap
metadata:
  name: config-product
data:
  application.yml: |-
                  spring:
                    application:
                      name: product
                    datasource:
                      url: jdbc:mysql://192.168.0.116:3306/product?createDatabaseIfNotExist=true
                      username: root
                      password: root
                    flyway:
                      locations:
                        - classpath:db/migration
                        - classpath:db/testdata
                    jpa:
                      hibernate:
                        ddl-auto: 'update'
                      properties:
                        hibernate:
                          dialect: org.hibernate.dialect.MySQL57Dialect
                      show-sql: true
                    management:
                    endpoints:
                      web:
                        exposure:
                          include: "*"
                    metrics:
                      export:
                        prometheus:
                          enabled: true
                    endpoint:
                      metrics:
                        enabled: false
                      prometheus:
                        enabled: true
